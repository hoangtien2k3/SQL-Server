//
- SQL WHERE Clause:
    + The WHERE clause is used to filter records.
    + It is used to extract only those records that fulfill a specified condition.

//
- WHERE Syntax:
    SELECT column1, column2, ...
    FROM table_name
    WHERE condition;


NOTE: The WHERE clause is not only used in SELECT statements, it is also used in UPDATE, DELETE, etc.!

//
- WHERE Clause Example:
    SELECT * FROM Customers
    WHERE Country='Mexico';

//
- Text Fields vs. Numeric Fields:
    SELECT * FROM Customers
    WHERE CustomerID=1;

//
- Operators in The WHERE Clause: các toán tử trong WHERE clause
    Operator	    Description	
    =	            Equal	
    >	            Greater than	
    <	            Less than	
    >=	            Greater than or equal	
    <=	            Less than or equal	
    <> or !=	    Not equal. Note: In some versions of SQL this operator may be written as !=	
    BETWEEN	        Between a certain range	: gữa một phạm vi nhất định
    LIKE	        Search for a pattern	
    IN	            To specify multiple possible values for a column

//
Ex: '='
    SELECT * FROM Products
    WHERE Price = 18;

    '>'
    SELECT * FROM Products
    WHERE Price > 30;

    '<'
    SELECT * FROM Products
    WHERE Price < 30;

    '>='
    SELECT * FROM Products
    WHERE Price >= 30;

    '<='
    SELECT * FROM Products
    WHERE Price > 30;

    '<>' or '!='
    SELECT * FROM Products
    WHERE Price <> 18; 

    'BETWEEN'
    SELECT * FROM Products
    WHERE Price BETWEEN 50 AND 60; =>  (50 <= Price <= 60)

    'LIKE'
    SELECT * FROM Customers
    WHERE City LIKE 's%';
    
    'IN'
    SELECT * FROM Customers
    WHERE City IN ('Paris','London'); => (City chỉ gồm 'Paris' và 'London')


    

    





